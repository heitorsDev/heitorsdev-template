{
  "name": "heitorsdev-template",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Template for TypeScript Node repos: Matt Pocock skill standards, Linux+Windows CI, branch-prefix semver releases.",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "node --test \"src/**/*.test.ts\"",
    "verify": "npm run typecheck && npm run lint && npm test"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@types/node": "^22.10.0",
    "eslint": "^9.39.5",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.20.0"
  }
}
